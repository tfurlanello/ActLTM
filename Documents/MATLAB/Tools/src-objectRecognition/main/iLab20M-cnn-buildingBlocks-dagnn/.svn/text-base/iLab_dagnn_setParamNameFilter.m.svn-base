function fname =  iLab_dagnn_setParamNameFilter(prefix)
    
    narginchk(1,1);
    if ~exist('prefix', 'var') || isempty(prefix) || ~ischar(prefix)
        error('Please use a string as input\n');
    end
    
    fname = sprintf('%sf', prefix);

end