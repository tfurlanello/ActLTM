function mName = iLab_dagnn_setParamNameBNmean(prefix)
    
    narginchk(1,1);
    
    if ~exist('prefix', 'var') || isempty(prefix) || ~ischar(prefix)
        error('Please use a string as input\n');
    end
    
    mName = sprintf('%sm', prefix);

end