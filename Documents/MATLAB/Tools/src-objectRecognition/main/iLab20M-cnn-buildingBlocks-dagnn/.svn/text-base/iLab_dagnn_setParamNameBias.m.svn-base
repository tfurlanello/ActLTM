function bname =  iLab_dagnn_setParamNameBias(prefix)
    
    narginchk(1,1);
    if ~exist('prefix', 'var') || isempty(prefix) || ~ischar(prefix)
        error('Please use a string as input\n');
    end
    
    bname = sprintf('%sb', prefix);
end